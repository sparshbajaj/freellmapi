version: '3.8'

services:
  freellmapi:
    image: ghcr.io/sparshbajaj/freellmapi:latest
    container_name: freellmapi
    ports:
      - "3001:3001"
    environment:
      - NODE_ENV=production
    restart: unless-stopped
